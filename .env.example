# ═══════════════════════════════════════════════
#  PastryStock Manager — Variables de Entorno
#  Copiar como .env y ajustar valores antes de
#  ejecutar docker-compose up
# ═══════════════════════════════════════════════

# ── PostgreSQL ──────────────────────────────────
POSTGRES_USER=pastry_user
POSTGRES_PASSWORD=REEMPLAZAR_CON_CONTRASENA_LOCAL
POSTGRES_DB=pastry_inventory

# ── Redis ───────────────────────────────────────
REDIS_PASSWORD=REEMPLAZAR_CON_CONTRASENA_LOCAL
REDIS_CACHE_TTL=3600

# ── Seguridad JWT ───────────────────────────────
# Generar con: openssl rand -hex 32
SECRET_KEY=REEMPLAZAR_CON_VALOR_GENERADO_POR_OPENSSL
ACCESS_TOKEN_EXPIRE_MINUTES=60

# ── Seed Superadministrador ─────────────────────
# NUNCA reutilizar estos valores en un despliegue público: este archivo es parte
# del repositorio. Definir una contraseña propia y fuerte en cada entorno.
SUPERADMIN_EMAIL=superadmin@mipasteleria.pe
SUPERADMIN_PASSWORD=REEMPLAZAR_CON_CONTRASENA_FUERTE

# ── NextAuth ────────────────────────────────────
# Generar con: openssl rand -base64 32
NEXTAUTH_SECRET=REEMPLAZAR_CON_VALOR_GENERADO_POR_OPENSSL
